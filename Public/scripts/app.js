var app = app || {};
app.

$("#dropdownMenu1").change(function() {
   $("#dropdownMenu2").load("Menu/" + $(this).val() + ".txt");
    switch ($("select[name='beerStyleCategories'] option:selected"))
   }
});

$("#dropMenu1 a").click(function() {
  menu_list = find(style_id from style_name)
  style_id = $(this).val()
});
